document.getElementById('f').addEventListener('submit',async e=>{e.preventDefault();let d=Object.fromEntries(new FormData(e.target));['src_bytes','dst_bytes','duration','src_packets','dst_packets','dst_port','failed_connections','login_attempts','connection_rate'].forEach(k=>d[k]=Number(d[k]));let r=await fetch('/predict',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(d)});let x=await r.json();document.getElementById('result').innerHTML='<b>Prediction:</b> '+x.prediction+' &nbsp; <b>Risk Score:</b> '+x.risk_score+'%';});
